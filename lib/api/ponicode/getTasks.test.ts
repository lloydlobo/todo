import axios from "axios";
import { URL_API } from "../../constants";
// @ponicode
describe("getTasks.getTasks", () => {
  test("1", async () => {
    const a = 1;
    expect(a).toBe(1);
    // const d = await axios.get(URL_API);
    // expect(d.data).toBeTruthy();
  });
});
